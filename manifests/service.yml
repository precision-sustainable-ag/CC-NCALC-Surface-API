apiVersion: v1
kind: Service
metadata:
    name: precisionsustainableagccncalcsurfaceapi
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: precisionsustainableagccncalcsurfaceapi